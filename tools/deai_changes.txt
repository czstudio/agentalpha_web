### agent-deployment-reliability #title
- 旧: Agent 上线不是把接口接通：超时、熔断和未知结果要先设计
- 新: Agent 上线，先把超时、熔断和未知结果设计好

### agent-handoff-contract #title
- 旧: 多智能体交接不是把聊天记录转发过去：先把合同写清楚
- 新: 多智能体交接：先把合同写清楚

### agent-memory-system #title
- 旧: 记忆系统不是聊天记录：短期、长期和压缩到底怎么分工
- 新: 记忆系统里，短期、长期和压缩到底怎么分工

### agent-project-evidence #title
- 旧: 项目讲不深，通常不是项目小而是证据链断了
- 新: 项目讲不深，通常是证据链断了

### agent-security-boundaries #title
- 旧: Agent 安全不是加一句提示词：权限、工具和数据边界怎么设计
- 新: Agent 安全怎么设计：权限、工具和数据边界

### llm-data-mixture #title
- 旧: 预训练数据不是越多越好：这批数据值得加吗？
- 新: 这批预训练数据值得加吗？

### llm-model-selection #title
- 旧: LLM 选型不是比一个总榜：把任务、成本和失败代价放进同一张表
- 新: LLM 选型：把任务、成本和失败代价放进同一张表

### llm-sft-data-quality #title
- 旧: SFT 不是把答案背下来：一条样本怎样改变模型行为
- 新: 一条 SFT 样本怎样改变模型行为

### multi-agent-protocol-state #title
- 旧: 多 Agent 不是群聊：消息和状态到底怎么管？
- 新: 多 Agent 的消息和状态到底怎么管？

### multimodal-to-transformer #title
- 旧: 多模态模型不是给图片加个输入框：图像怎样进入 Transformer？
- 新: 图像怎样进入 Transformer？

### rag-graph-retrieval #title
- 旧: Graph RAG 不只是画关系图：证据要能沿路径找回去
- 新: Graph RAG：证据要能沿关系路径找回去

### rag-query-rewrite #title
- 旧: RAG 查询改写不是把问题说长：别替用户补条件
- 新: RAG 查询改写：别替用户补条件

### rag-retrieval-pipeline #title
- 旧: RAG 不只是“向量库 + 提示词”：证据怎样一路到答案？
- 新: RAG 的证据怎样一路到答案？

### tool-function-contract #title
- 旧: Function Calling 不是模型会调函数就完事：先把契约验清楚
- 新: Function Calling：先把契约验清楚

### agent-context-engineering #excerpt
- 旧: Agent 变笨，多数不是模型小了，而是上下文里混进旧状态、没用的工具结果、没人核过的指令。先筛，再压缩、排序、标注，模型才看得见重点。
- 新: Agent 变笨，多数是上下文里混进旧状态、没用的工具结果、没人核过的指令。先筛，再压缩、排序、标注，模型才看得见重点。

### agent-core-architecture #excerpt
- 旧: 面试官问 Agent 有哪些核心组件，不是等你背清单。他要看的是你能不能把目标、状态、行动、观察、停止条件串成一条真跑得起来的流程。
- 新: 面试官问 Agent 有哪些核心组件，看的是你能不能把目标、状态、行动、观察、停止条件串成一条真跑得起来的流程。

### agent-deployment-reliability #excerpt
- 旧: 线上故障往往不是模型单点崩，而是工具超时、重复写入、上下文膨胀、版本漂移撞一起。先画状态和故障边界，再谈扩容。
- 新: 线上故障往往是工具超时、重复写入、上下文膨胀、版本漂移撞一起。先画状态和故障边界，再谈扩容。

### agent-failure-story #excerpt
- 旧: 失败案例不是自我否定题，考的是边界感和复盘。挑一个真实发生过的，把现象、影响、定位、修复、还剩什么风险讲清楚，既不甩锅，也不把责任全揽在自己身上。
- 新: 失败案例考的是边界感和复盘。挑一个真实发生过的，把现象、影响、定位、修复、还剩什么风险讲清楚，既不甩锅，也不把责任全揽在自己身上。

### agent-human-escalation #excerpt
- 旧: Agent 卡住时，最怕的不是它停下来，而是证据、权限、确认都没拿到，还照样自信地往下做。升级人工的条件、交接的资料、之后的恢复方式要写清楚，人工接管才不会只剩一句“请联系客服”。
- 新: Agent 卡住时，最怕的是证据、权限、确认都没拿到，还照样自信地往下做。升级人工的条件、交接的资料、之后的恢复方式要写清楚，人工接管才不会只剩一句“请联系客服”。

### agent-planning-reflection #excerpt
- 旧: Planner 不是拆得越细越聪明，Reflection 也不是补一句“你确定吗”。计划得能落地执行，反馈得真能改方向，什么时候收手也要提前定好。
- 新: Planner 要拆到能落地执行，拆得细不等于聪明；Reflection 要能改方向，补一句“你确定吗”不算反思。什么时候收手也要提前定好。

### agent-project-evidence #excerpt
- 旧: 面试官追你的项目，不是想听组件清单。他要听的是你碰上过什么问题、做了什么决定、怎么验证、最后结果如何。小项目照样能讲出深度。
- 新: 面试官追你的项目，要听的是你碰上过什么问题、做了什么决定、怎么验证、最后结果如何。小项目照样能讲出深度。

### agent-rag-why #excerpt
- 旧: RAG 不是 Agent 标配。知识会更新、数据不能公开、答案必须带出处、上下文装不下时，检索才值得加。
- 新: 知识会更新、数据不能公开、答案必须带出处、上下文装不下时，检索才值得加。

### agent-system-design-interview #excerpt
- 旧: 系统设计面试不是比谁画的服务框多。先问清楚用户是谁、成功标准是什么、哪些边界不能碰，再把状态、工具、证据、评测、上线风险一项项接上。
- 新: 系统设计面试，先问清楚用户是谁、成功标准是什么、哪些边界不能碰，再把状态、工具、证据、评测、上线风险一项项接上。

### agent-workflow-vs-agent #excerpt
- 旧: Workflow 和 Agent 不是谁更先进的二选一。先看任务不确定性、风险、验证成本、回退办法，再定哪些步骤交给 Agent，哪些边界写死。
- 新: Workflow 和 Agent 怎么选？先看任务不确定性、风险、验证成本、回退办法，再定哪些步骤交给 Agent，哪些边界写死。

### agentic-rl-reward-hacking #excerpt
- 旧: Reward hacking 不是模型学坏了，是验收器给了个更好钻的空子。Agent 越擅长用工具，越得把奖励和真实结果分开核对。
- 新: Reward hacking，是验收器给了个更好钻的空子。Agent 越擅长用工具，越得把奖励和真实结果分开核对。

### llm-long-context #excerpt
- 旧: 长上下文的瓶颈，往往不在能塞多少 token，而在关键事实能不能被定位、留下、引用。把上下文预算拆给任务、证据、工具、输出，再用中间信息测一测，才谈得上长文能力。
- 新: 长上下文的瓶颈，往往在关键事实能不能被定位、留下、引用。把上下文预算拆给任务、证据、工具、输出，再用中间信息测一测，才谈得上长文能力。

### llm-rlhf-reward-model #excerpt
- 旧: RLHF 不是喂“人类喜欢”，而是把偏好学成奖励，再用策略优化推高质量。数据、奖励模型、KL 约束、PPO 串起来，才讲得清它怎么工作、怎么被 reward hacking 反噬。
- 新: RLHF 先把偏好学成奖励，再用策略优化推高质量。数据、奖励模型、KL 约束、PPO 串起来，才讲得清它怎么工作、怎么被 reward hacking 反噬。

### multi-agent-concurrency-budget #excerpt
- 旧: 并发不是 Agent 数量直接相乘。共享资源、复制上下文、尾延迟，都会让协作越多越慢；真正要管的是任务拓扑、扇出、token 和失败重试。
- 新: 共享资源、复制上下文、尾延迟，都会让协作越多越慢；真正要管的是任务拓扑、扇出、token 和失败重试。

### multi-agent-task-decomposition #excerpt
- 旧: 多 Agent 的价值不是复制几个聊天窗口，而是把互相牵制的目标拆成可验收的责任边界。先判断值不值得拆，再定角色、交接和失败回退。
- 新: 多 Agent 的价值在于把互相牵制的目标拆成可验收的责任边界。先判断值不值得拆，再定角色、交接和失败回退。

### multimodal-rag #excerpt
- 旧: 多模态 RAG 不是给文本 RAG 添一列 image_url 就完了。文字、表格、图片、坐标得组织成统一的证据对象，再靠跨模态召回、元数据过滤、引用回溯去回答问题。
- 新: 多模态 RAG 得把文字、表格、图片、坐标组织成统一的证据对象，再靠跨模态召回、元数据过滤、引用回溯去回答问题。

### multimodal-vision-slicing #excerpt
- 旧: 小字、表格、长截图答不对，多半不是模型不行，而是目标没带着合适的分辨率进入上下文。全图定位、局部切片、OCR、证据合并配合着用，视觉问答才稳。
- 新: 小字、表格、长截图答不对，多半是目标没带着合适的分辨率进入上下文。全图定位、局部切片、OCR、证据合并配合着用，视觉问答才稳。

### rag-chunking-strategy #excerpt
- 旧: 分块不是切得整齐就行。每一块都得两头兼顾：检索时能被准确定位，单独拎出来也撑得住一个回答。
- 新: 每一块都得两头兼顾：检索时能被准确定位，单独拎出来也撑得住一个回答。

### rag-query-rewrite #excerpt
- 旧: Query rewrite、问题拆解、假设文档，都是把用户的话翻成知识库听得懂的说法，不是让模型另起一个问题。每次改写都要能回到原问，必要时能撤回。
- 新: Query rewrite、问题拆解、假设文档，都是把用户的话翻成知识库听得懂的说法。每次改写都要能回到原问，必要时能撤回。

### rag-rerank-and-hybrid #excerpt
- 旧: 向量检索负责把可能相关的证据捞进候选集，重排再把真能回答问题的片段顶到前面。一个管找得全，一个管排得准，不是二选一。
- 新: 向量检索负责把可能相关的证据捞进候选集，重排再把真能回答问题的片段顶到前面。一个管找得全，一个管排得准。

### rag-vector-database-selection #excerpt
- 旧: 向量库不是 RAG 的魔法核心。先定语料规模、过滤条件、更新频率、召回目标、尾延迟，再比较 HNSW、倒排、混合检索和托管服务。
- 新: 先定语料规模、过滤条件、更新频率、召回目标、尾延迟，再比较 HNSW、倒排、混合检索和托管服务。

### tool-permission-audit #excerpt
- 旧: Agent 的权限不是发一个 API key 就完事。主体、租户、资源、动作、审批、幂等、审计证据都要进同一条调用链，这样才说得清谁在什么条件下让系统干了什么。
- 新: Agent 的主体、租户、资源、动作、审批、幂等、审计证据都要进同一条调用链，这样才说得清谁在什么条件下让系统干了什么。

### tool-retry-policy #excerpt
- 旧: 失败不是一个简单的真或假。先看请求到没到、外部副作用是否未知、参数有没有错，再决定是重试、换工具、回退，还是转人工。
- 新: 失败，先看请求到没到、外部副作用是否未知、参数有没有错，再决定是重试、换工具、回退，还是转人工。

### tool-streaming-gateway #excerpt
- 旧: 工具流式传输不是把文字切成小段就完事，是把一次调用拆成能排序、能恢复的事件流。顺序、背压、断线续传、权限、最终提交都归网关管，体验和可靠性才不会打架。
- 新: 工具流式传输，是把一次调用拆成能排序、能恢复的事件流。顺序、背压、断线续传、权限、最终提交都归网关管，体验和可靠性才不会打架。

